kind = "web"
previewPath = "/"
title = "FreshCheck AI"
version = "1.0.0"
id = "artifacts/freshcheck-ai"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19012

[services.development]
run = "pnpm --filter @workspace/freshcheck-ai run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/freshcheck-ai", "run", "build" ]
publicDir = "artifacts/freshcheck-ai/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19012"
BASE_PATH = "/"
